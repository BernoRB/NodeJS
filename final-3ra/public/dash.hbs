<div class="container-fluid px-0">
    <nav class="navbar navbar-expand-md navbar-dark bg-black px-0">
        <a class="navbar-brand mr-5" href="#" id="navbarDropdown"><img id="logo" src="{{avatar}}">
            &nbsp;&nbsp;&nbsp;{{username}} ({{email}}) </a>

        <button class="navbar-toggler mr-3" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/productos">Productos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Mi cuenta</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/carrito/{{cartId}}/productos">Carrito</a>
                </li>
                {{#if isAdmin}}
                <li class="nav-item">
                    <a class="nav-link" href="/productos/addProd">Agregar Prod</a>
                </li>
                {{/if}}
                <li class="nav-item" style="float:right;">
                    <a class="nav-link btn btn-primary" href="/logout">Logout</a>
                </li>
            </ul>
        </div>
    </nav>
</div>



<div class="container mb-5 mt-5">
    <div class="row">
        {{#each products}}
        <div class="col-md-3">
            <div class="card mt-3 border">
                <div class="align-items-center text-center">
                    <img src="{{this.thumbnail}}" class="rounded" width="160">
                    <h5>{{this.title}}</h5>
                    <div class="mt-3 info">
                        <span class="text1 d-block">{{this.desc}}</span>
                    </div>
                    <div class="cost mt-3 text-dark">
                        <span>${{this.price}}</span>
                    </div>

                    <!--Al clickear, va a la ruta segun el cart id. Se pasa por body el id del prod que eligio-->
                    <form action="/carrito/{{../cartId}}/productos" method="post">
                        <input type="hidden" name="productId" value={{this._id}} />
                        <button type="submit" class="btn btn-outline-danger btn-sm mt-3 mb-3">AÑADIR AL CARRO</button>
                    </form>

                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>